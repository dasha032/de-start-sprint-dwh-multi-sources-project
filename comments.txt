Привет, Егор! Рада, что ты проверяешь мой проект. Очень нравятся вебинары, которые ты проводишь!

У меня возникло несколько вопросов по проекту и спринту в целом.

1. Я запуталась в инкрементальной загрузке и загрузке "снэпшотами".
Как я поняла, в случае со справочниками используется загрузка полной таблицы, а в случае,
например, с таблицей заказов или доставок используем инкрементальную загрузку.

Тогда при загрузке полной таблицы не нужен BATCH_SIZE и фильтровать данные в таблице,
например, WHERE id > "threshold" не нужно?

2. Также не разобралась с паттерном Transactional Outbox. В уроках говорится, что мы с ним познакомимся,
но явно про него мало написано.

3. Запуталась с тем, как заполнять таблицу с прогрессом, для некоторых таблиц мы там сохраняем максимальный загруженный id,
для некоторых дату. Как правильно обновлять эту таблицу в случае с загрузкой снэпшотом? В прогресс всегда будет записываться одно и то же значение максимального id получается? 

И как заполнять такую таблицу в stg слое, данные там хранятся as is и таблицы бывают без суррогатного первичного ключа id, а, например, 
только courier_id и courier_name, нужно ли добавлять поле id или фильтровать по другому?

4. В слое витрин нужно ли создавать таблицу, куда будет сохраняться прогресс? И при создании витрины нужно ли использовать
фильтрацию данных, например, WHERE t.ts > %(last_date)s? Как я понимаю, витрина загружается всегда полностью, а не инкрементально

5. Еще вопрос по поводу таблиц в слое dds: правильно ли я их создала и правильно ли то, что я добавила сущности "Курьер" и 
"Доставка" в таблицу с заказами?

